# File: netlify.toml
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# SPA routing for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
